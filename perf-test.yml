---
execution:
- concurrency: 100
  ramp-up: 1m
  hold-for: 1m30s
  scenario: simple
                  
scenarios:
  simple:
    think-time: 0.001
    requests:
    - http://localhost:5000/

settings:
  default-executor: gatling
